name: helpers
description: A simple collection of helpers.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  printer:
    git:
      url: https://github.com/FredslundMagnus/printer.git
  painter:
    git:
      url: https://github.com/FredslundMagnus/painter.git
  extensions:
    git:
      url: https://github.com/FredslundMagnus/extensions.git

dev_dependencies:
  pedantic:
